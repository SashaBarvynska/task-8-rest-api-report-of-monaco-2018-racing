parameters:
  - in: path
    name: driver_code
    type: string
    required: false
  - name: format
    in: query
    type: string
    enum: ['json', 'xml']
    required: true
    default: 'json'
responses:
  200:
    description: OK
